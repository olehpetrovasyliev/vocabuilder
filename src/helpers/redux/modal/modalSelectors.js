export const selectIsModalAddOpen = (state) => state.modal.isModalAddOpen;
export const selectIsModalEditOpen = (state) => state.modal.isModalEditOpen;
export const selectIsModalWellDoneOpen = (state) =>
  state.modal.isModalWellDoneOpen;
